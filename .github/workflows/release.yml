on:
  push:
#    tags:
#      - '*'

name: Create Release

jobs:
  build:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - run: apt update && apt install python3-setuptools
      - run: pip3 install niet
        
